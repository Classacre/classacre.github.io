# mikamartin — personal site

A Lando Norris-style personal site for Mika Martin Nieuwenhuyzen, hand-rolled
with plain HTML/CSS/JS. No build step, no framework. Hosted on GitHub Pages.

Live at https://classacre.github.io/

## Stack

- Vanilla JS + CSS. Libraries loaded from CDN:
  - [Lenis](https://lenis.darkroom.engineering/) — smooth scroll
  - [Three.js](https://threejs.org/) — WebGL (depth-map hero portrait, DNA helix)
- Pages: `index.html` (home), `research/`, `projects/`, `about/`
- `js/main.js` — shared engine: fullscreen menu, page transitions ("Load Martin"
  signature curtain), marquees, reveals, counters, manifesto word-highlight,
  horizontal gallery, custom cursor, easter eggs (console log; type A C G T)
- `js/hero3d.js` — home hero: photo + depth map shader, mouse parallax
- `js/dna3d.js` — research: procedural DNA helix, scroll-driven
- `js/pages/*.js` — per-page init/dispose

## Local preview

```
python -m http.server 8000
```

then open http://localhost:8000/ (ES modules need a server; file:// won't work).

---

# WHAT TO REPLACE (placeholder inventory)

## Already real

- `portrait.png` — graduation photo, keyed onto the site background (960×1280).
  Used in the WebGL hero, the About page, the menu image column and the og card.
  Regenerated from the source photo by `../process_photo.py` — drop a new photo
  over `D:\Downloads\fal_image.png` (or edit SRC) and rerun to rebuild all three
  derived files. Keep the subject on a plain or transparent background.
- `portrait-depth.png` — depth map derived from the photo (white = near). The
  hero 3D effect reads this. Regenerated by the same script. If you supply your
  own portrait, make a matching depth map (Depth Anything / Marigold), or ask.
- `og.png` — social share card (1200×630), built by the same script.

## Still placeholders (real photos wanted)

All in `assets/img/`, all 1200×900 (4:3). Replace with your own photos using
the **same filename** — nothing else to change.

Already real: `card-paper.png` (screenshot of the paper's cell.com page),
`paper-figure.png` (Figure 1 from the paper, shown on card hover),
`card-nodes/agent/doi/multiplayer/cloud/mcp.png` (real BioNodulo app
screenshots — agent/DOI/multiplayer are stand-ins since those features render
as live demos, not static images), `bionodulo-wide.png` (single-cell canvas,
home gallery), `surabaya.png` (Tugu Pahlawan, Wikimedia Commons).

| File | Used for | Suggested real photo |
|---|---|---|
| `card-gel.png` | gallery ("UWA, 2024") | lab work, gels, screens with data |
| `card-dna.png` | menu image (Research) | plants / Arabidopsis / lab bench |
| `card-karate.png` | "In person" card, about hover, menu | karate photo (currently a skateboarder) |
| `card-agent.png` | BioNodulo AI agent | screenshot of the agent building a graph |
| `card-doi.png` | DOI-to-pipeline feature | screenshot of the DOI feature |
| `card-multiplayer.png` | BioNodulo multiplayer/MCP | screenshot of multiplayer editing |
| `card-mcp.png` | node library / MCP | screenshot of Claude driving the editor |

## Not files, but also swappable

- **Signature SVG** — the drawing in the loader, the page-transition curtain
  (`js/main.js` `SIG_PATHS`) and the home signature section (`index.html`
  `.sig-wrap`). All use the same five paths; replace the `d` attributes with
  paths traced from your real signature (keep `viewBox="0 0 700 260"`).
- **Loader pun** — "Load Martin®" sits at the bottom of the loader in each
  HTML file, and `load martin` in `js/main.js` (`curtainHTML`).
- **Rotating badge text** — home hero, `index.html`, the `<textPath>` content.
- **Next milestone card** — home hero; date logic in `js/pages/home.js`
  (currently counts down to 1 Feb 2027).

## Socials

Footer + menu list: GitHub, LinkedIn, Google Scholar, ORCID, Instagram
(@mikamartinn), Discord (classacre — copies handle to clipboard on click).
Edit in the four HTML footers and the `#menu` block.

## Deploy

Push to `main`. GitHub Pages serves the repo root (`.nojekyll` present).
