// frontend/src/components/ChatPanel.tsx
import React from 'react';

const ChatPanel = () => {
  return (
    <div>
      <h1>Chat Panel</h1>
      {/* TODO: Implement Agent Chat (OpenRouter, streaming) */}
    </div>
  );
};

export default ChatPanel;